version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
      - "11434:11434"
    environment:
      - API_KEY=${API_KEY:-devkey}
      - DEV_MODE=true
    volumes:
      - ./backend:/app
      - ollama-data:/root/.ollama
    restart: unless-stopped

volumes:
  ollama-data:
